from datetime import datetime       # importujemy datetime, aby móc pracować z datami i czasem

def save_result_to_file(weight_kg: float, height_m: float, bmi_value: float, category: str, filename: str):